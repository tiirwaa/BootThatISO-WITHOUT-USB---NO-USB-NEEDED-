# BootThatISO!

**[🇺🇸 English](README.md) | [🇪🇸 Español](README.es.md) | [🇧🇷 Português](README.pt.md) | [🇩🇪 Deutsch](README.de.md) | [🇫🇷 Français](README.fr.md) | [🇮🇹 Italiano](README.it.md)**

<div style="display: flex; justify-content: center; align-items: center;">
<img src="res/logo.png" alt="Logo" style="margin-right: 20px;">
<img src="res/ag.png" alt="Company Logo">
</div>

## Videos

- [Hirens Boot — No USB Needed — Boot ISO Directly (No USB Required)](https://www.youtube.com/watch?v=RaRJTmek0W8)
- [Install Windows 10/11 Without USB | BootThatISO (No Flash Drive Needed!)](https://www.youtube.com/watch?v=Fo6poEWZNew)

BootThatISO! — это инновационный инструмент Windows, который позволяет **загружать операционные системы из файлов ISO без необходимости использования USB-накопителя**. Идеально подходит для ситуаций, когда у вас нет под рукой USB-устройства, например, во время путешествий, при использовании заимствованного оборудования или в экстренных случаях. Автоматизирует создание разделов EFI и данных на внутреннем диске, прямое чтение ISO и извлечение файлов, а также настройку BCD, предлагая интуитивный графический интерфейс и поддержку автоматического выполнения.

Эта утилита особенно полезна для:
- **Быстрых Установок**: Прямая загрузка с ISO для установки Windows, Linux или инструментов восстановления без подготовки USB.
- **Тестовых Сред**: Тестирование ISO ОС или утилит без изменения внешнего оборудования.
- **Восстановления Системы**: Доступ к инструментам восстановления, таким как HBCD_PE или живым средам без зависимости от внешних носителей.
- **Автоматизации**: Интеграция в скрипты для массовых развертываний или автоматизированных конфигураций.

Разработано **Andrey Rodríguez Araya**.

Веб-сайт: [English](https://agsoft.co.cr/en/software-and-services/) | [Español](https://agsoft.co.cr/servicios/)

![Screenshot](screenshot.png?v=1)

![Boot screen](boot_screen.png?v=1)

## Основные Возможности
- Создает или переформатирует разделы `ISOBOOT` (данные) и `ISOEFI` (EFI) на системном диске с опциями формата FAT32, exFAT или NTFS.
- Поддерживает два режима загрузки: полная загрузка ISO на диск или режим RAMDisk (boot.wim в памяти).
- Обнаруживает ISO Windows и автоматически настраивает конфигурацию BCD; ISO не-Windows загружаются напрямую с раздела EFI.
- Выполняет дополнительные проверки целостности (`chkdsk`), генерирует подробные журналы и позволяет отменить или восстановить пространство.
- Предоставляет режим без контроля для интеграции скриптов через аргументы командной строки.
- **Кэш хэша ISO (ISOBOOTHASH)**: Сравнивает MD5 ISO, выбранный режим загрузки и формат со значениями, хранящимися в файле `ISOBOOTHASH` на целевом диске. Если они совпадают, пропускает форматирование и копирование содержимого для ускорения повторных запусков.

## Протестированные ISO

### Режим RAM (Загрузка из Памяти)
- ✅ HBCD_PE_x64.iso (ПОЛНОСТЬЮ ФУНКЦИОНАЛЕН - Загружает все программы из RAM)
- ✅ Win11_25H2_Spanish_x64.iso (ПОЛНОСТЬЮ ФУНКЦИОНАЛЕН - Загрузка и Установка)
- ✅ Windows10_22H2_X64.iso (ПОЛНОСТЬЮ ФУНКЦИОНАЛЕН - Загрузка и Установка)

### Режим EXTRACT (Полная Установка)
- ✅ HBCD_PE_x64.iso (возврат к ISOBOOT_RAM)
- ✅ Win11_25H2_Spanish_x64.iso (возврат к ISOBOOT_RAM)
- ✅ Windows10_22H2_X64.iso (возврат к ISOBOOT_RAM)

## Требования
- Windows 10 или 11 64-bit с правами администратора.
- Минимум 12 ГБ свободного места на диске `C:` для создания и форматирования разделов (инструмент пытается уменьшить на 12 ГБ).
- PowerShell, DiskPart, bcdedit и доступные инструменты командной строки Windows.
- Для компиляции: Visual Studio 2022 с CMake. Внешний менеджер пакетов не требуется; SDK 7‑Zip включен в `third-party/`.

## Компиляция
```powershell
# Настройка и сборка (VS 2022, x64)
cmake -S . -B build -G "Visual Studio 17 2022" -A x64
cmake --build build --config Release
```

Конечный исполняемый файл находится в `build/Release/BootThatISO!.exe`. Также включен `compilar.bat` с эквивалентными шагами.

### Быстрая компиляция с compilar.bat (рекомендуется)
```powershell
# В корне репозитория
./compilar.bat
```

## Использование
### Графический Интерфейс
1. Запустите `BootThatISO!.exe` **от имени администратора** (манифест уже запрашивает это).
2. Выберите файл ISO и формат файловой системы для `ISOBOOT`.
3. Определите режим загрузки:
   - `Полная Установка`: копирует все содержимое ISO на диск.
   - `Загрузка из Памяти`: копирует `boot.wim` и зависимости для загрузки из RAM.
4. Решите, выполнять ли `chkdsk` (снятие флажка пропускает проверку для ускорения процесса).
5. Нажмите **Создать Загрузочный Раздел** и отслеживайте прогресс через основную полосу, детальную полосу и панель журнала.
6. По завершении появится подтверждение перезагрузки. Используйте кнопку **Восстановить Пространство**, если нужно удалить разделы `ISOBOOT`/`ISOEFI` и расширить `C:`.
7. Кнопка **Службы** открывает страницу поддержки `https://agsoft.co.cr/servicios/`.

### Режим Без Контроля
Запустите двоичный файл с повышенными правами и следующими аргументами:

```
BootThatISO!.exe ^
  -unattended ^
  -iso="C:\путь\образ.iso" ^
  -mode=RAM|EXTRACT ^
  -format=NTFS|FAT32|EXFAT ^
  -chkdsk=TRUE|FALSE ^
  -autoreboot=y|n ^
  -lang=en_us|es_cr|ru_ru
```

- `-mode=RAM` активирует режим *Загрузка из Памяти* и копирует `boot.wim`/`boot.sdi`.
- `-mode=EXTRACT` соответствует *Полной Установке*.
- `-chkdsk=TRUE` принудительно выполняет проверку диска (по умолчанию опущено).
- `-lang` устанавливает код языка, соответствующий файлам в `lang/`.
- `-autoreboot` доступен для будущих автоматизаций; в настоящее время только регистрирует предпочтение.

Процесс регистрирует события и завершается без отображения главного окна.

## Журналы и Диагностика
Все операции создают файлы в `logs/` (создаются рядом с исполняемым файлом). Среди наиболее релевантных:
- `general_log.log`: общая хронология событий и сообщений UI.
- `diskpart_log.log`, `reformat_log.log`, `recover_script_log.txt`: шаги разбиения на разделы и переформатирования.
- `iso_extract_log.log`, `iso_content.log`: детали извлеченного содержимого ISO.
- `bcd_config_log.log`: команды и результаты конфигурации BCD.
- `copy_error_log.log`, `iso_file_copy_log.log`: копирование файлов и ошибки.

Просматривайте эти журналы при диагностике сбоев или обмене отчетами.

## Безопасность и Восстановление
- Операция изменяет системный диск; сделайте резервную копию перед выполнением инструмента.
- Во время процесса избегайте закрытия приложения через Диспетчер задач; используйте встроенную опцию отмены.
- Используйте кнопку **Восстановить Пространство** для удаления разделов `ISOBOOT`/`ISOEFI` и восстановления диска `C:`, если решите отменить конфигурацию.

## Ограничения
- Работает на Диске 0 и уменьшает том C: на ~10,5 ГБ; другие компоновки дисков в настоящее время не поддерживаются.
- Требует прав администратора и доступности Windows PowerShell.
- Файлы языков в `lang/` обязательны; приложение показывает ошибку, если они не найдены.

## Благодарности
Разработано **Andrey Rodríguez Araya** в 2025 году.

## Лицензия
Этот проект находится под Лицензией GPL 3.0. См. файл `LICENSE` для получения дополнительной информации.

## Уведомления третьих сторон
- SDK 7‑Zip: Части этого продукта включают код из SDK 7‑Zip Игоря Павлова.
  - Краткое описание лицензирования (согласно `third-party/DOC/License.txt`):
    - Большинство файлов лицензированы под GNU LGPL (v2.1 или позже).
    - Некоторые файлы находятся в общественном достоянии, где это явно указано в заголовках.
    - `CPP/7zip/Compress/LzfseDecoder.cpp` находится под лицензией BSD 3‑Clause.
    - `CPP/7zip/Compress/Rar*` находятся под GNU LGPL с "ограничением лицензии unRAR".
  - Мы включаем минимальное подмножество (обработчик ISO и общие утилиты). Код RAR не используется этим проектом.
  - Полные тексты: см. `third-party/DOC/License.txt`, `third-party/DOC/lzma.txt` и `third-party/DOC/unRarLicense.txt`.
